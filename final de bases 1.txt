Create table PARQUE
(
	NUMERO Char(20) NOT NULL  UNIQUE ,
	NOMBRE VARCHAR NOT NULL,
	TELEFONO VARCHAR,
	DIRECCIÓN VARCHAR NOT NULL,
	COD_ADM VARCHAR NOT NULL,
	COD_V VARCHAR NOT NULL,
	COD_A VARCHAR NOT NULL,
	COD_F VARCHAR NOT NULL
);
Create table VEGETAL
(
	CODIGO_V Char(20) NOT NULL ,
	TIPO_V VARCHAR NOT NULL ,
	NOMBRE_V VARCHAR NOT NULL  
);
Create table ANIMAL
(
	CODIGO_A VARCHAR NOT NULL UNIQUE,
	TIPO VARCHAR NOT NULL,
	CANTIDAD INT NOT NULL,
	NOMBRE VARCHAR 
);
create table FUNCIONARIO(
	COD_TRAB VARCHAR NOT NULL UNIQUE,
	CEDULA_TRAB VARCHAR NOT NULL UNIQUE,
	NOMBRE VARCHAR NOT NULL
);
create table ADMINISTRADOR(	
	COD_ADM VARCHAR NOT NULL UNIQUE,
	CEDULA_ADM VARCHAR UNIQUE,
	NOMBRE VARCHAR NOT NULL,
	NUM_PARQUES INT
);
create table PARQUE_ADMINISTRADOR(
	COD_ADMIN varchar NOT NULL,
	NUMERO varchar NOT NULL
);
create table PARQUE_VEGETAL(
	NUMERO varchar NOT NULL,
	CODIGO_V INT NOT NULL,
	E_DOMINANTES INT NOT NULL
);
create table PARQUE_ANIMAL(
	CODIGO_A VARCHAR NOT NULL,
	NUMERO VARCHAR NOT NULL
);
Create table VISITANTE
(
	CEDULA INT NOT NULL PRIMARY KEY ,
	MOTIVO VARCHAR,
	FECHA_VISITA DATE, 
	NOMBRE VARCHAR,
	EDAD INT
);
Alter table PARQUE add primary key (NUMERO);
Alter table VEGETAL add primary key (CODIGO_V);
Alter table ANIMAL add primary key (CODIGO_A);
Alter table FUNCIONARIO add primary key (COD_TRAB);
Alter table ADMINISTRADOR add primary key (COD_ADM);
Alter table PARQUE_ADMINISTRADOR ADD CONSTRAINT PARQ_ADMIN_PK PRIMARY KEY (COD_ADMIN, NUMERO);
Alter table PARQUE_VEGETAL ADD CONSTRAINT PARQ_VEGETAL_PK PRIMARY KEY (NUMERO, CODIGO_V);
alter table PARQUE_ANIMAL ADD CONSTRAINT PARQ_ANIMAL_PK PRIMARY KEY (CODIGO_A, NUMERO);
Alter table PARQUE ADD FOREIGN KEY (COD_ADM) REFERENCES ADMINISTRADOR (COD_ADM);
Alter table PARQUE ADD FOREIGN KEY (COD_V) REFERENCES VEGETAL (CODIGO_V);
Alter table PARQUE ADD FOREIGN KEY (COD_A) REFERENCES ANIMAL (CODIGO_A);
Alter table PARQUE ADD FOREIGN KEY (COD_F) REFERENCES FUNCIONARIO (COD_TRAB);
Alter table PARQUE_ADMINISTRADOR ADD FOREIGN KEY (COD_ADMIN) REFERENCES ADMINISTRADOR;
Alter table PARQUE_ADMINISTRADOR ADD FOREIGN KEY (NUMERO) REFERENCES PARQUE;
alter table PARQUE_ANIMAL ADD FOREIGN KEY (CODIGO_A) REFERENCES ANIMAL;
alter table PARQUE_ANIMAL ADD FOREIGN KEY (NUMERO) REFERENCES PARQUE;

